/**
 * Capacidades reales del panel que se muestran en el login (admin/login.jsx).
 * Solo se listan funciones que el sistema tiene implementadas: roles/permisos,
 * la bitácora de `support/logs` y el límite de sesiones por cuenta.
 */
export const LOGIN_FEATURES = [
  "Roles y permisos granulares por módulo",
  "Bitácora de auditoría de cada cambio",
  "Sesiones simultáneas controladas por cuenta",
];

export const LOGIN_SUPPORT_EMAIL = "soporte@proyectofractal.com";
