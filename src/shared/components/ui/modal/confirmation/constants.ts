import type { ButtonCoreProps } from "@/shared/components/type";

export const DEFAULT_CANCEL: ButtonCoreProps = {
  label: "Cerrar",
  severity: "contrast",
};

export const DEFAULT_CONFIRM: ButtonCoreProps = {
  label: "Confirmar",
  severity: "contrast",
  variant: "outlined",
};
