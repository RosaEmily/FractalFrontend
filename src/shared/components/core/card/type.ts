import type { CardProps } from "primevue/card";

export interface CardCoreProps {
  dt?: CardProps["dt"];
  pt?: CardProps["pt"];
  ptOptions?: CardProps["ptOptions"];
  unstyled?: CardProps["unstyled"];
}
